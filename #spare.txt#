#!/usr/bin/env node 
import inquirer from 'inquirer'; 
import pensador from 'pensador-api';
import readline from 'readline';
import chalk from 'chalk';
import chalkAnimation from 'chalk-animation'; 

// const readline = require("readline");
// import chalk from 'chalk'/
const sleep = (ms = 1000) => new Promise((r) => setTimeout(r,ms)); 

async function welcome(){
    const rainbowTitle = chalkAnimation.rainbow('Testing \n' ); 

    await sleep();
    rainbowTitle.stop()
    
}  

async function askSearch(){
    const answers = await inquirer.prompt({
	name: 'query',
	type: 'input',
	message: 'What do you want to search?', 
	default(){
	    return 'amor'; 
	}, 
    })
    return  answers.query; 
    
}
async function search(query){
    let  result =  await pensador({term:query, max:3});
    return result;
} 
    
 
welcome()
let answer  = askSearch();
console.log(answer);
   
let  result = search(answer) 
console.log(result);
// let array =[]; 
// for(let i = 0; i < result[0]["phrases"].lenght; i++){
// 	array.push(result[0]["phrases"][i]["text"]); 
//     }
    
